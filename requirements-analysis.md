# CVLab 需求分析文档

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-18 | 初始版 |

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [设计原则](#2-设计原则)
3. [功能需求总览](#3-功能需求总览)
4. [模块一：系统环境检测与跨平台适配](#4-模块一系统环境检测与跨平台适配)
5. [模块二：智能 Batch Size 管理](#5-模块二智能-batch-size-管理)
6. [模块三：非侵入式模型接入协议](#6-模块三非侵入式模型接入协议)
7. [模块四：实验追踪与版本控制](#7-模块四实验追踪与版本控制)
8. [模块五：训练过程诊断（CV 专项）](#8-模块五训练过程诊断cv-专项)
9. [模块六：超参管理与调优](#9-模块六超参管理与调优)
10. [模块七：数据集管理](#10-模块七数据集管理)
11. [模块八：Checkpoint 管理](#11-模块八-checkpoint-管理)
12. [模块九：实验对比与报告](#12-模块九实验对比与报告)
13. [模块十：数据 I/O 瓶颈诊断](#13-模块十数据-io-瓶颈诊断)
14. [模块十一：模型 Profiling](#14-模块十一模型-profiling)
15. [模块十二：预训练权重管理](#15-模块十二预训练权重管理)
16. [模块十三：数据集版本与血缘管理](#16-模块十三数据集版本与血缘管理)
17. [非功能性需求](#17-非功能性需求)
18. [功能优先级矩阵](#18-功能优先级矩阵)
19. [技术风险与关键决策](#19-技术风险与关键决策)

---

## 1. 项目背景与目标

### 1.1 背景

计算机视觉（CV）研究者/学习者在日常实验管理中面临大量重复性、易出错的工程工作：

- **环境配置碎片化**：每次重装或换机器都要重新配环境，不同 OS（Windows/Linux/WSL2）下的 DataLoader 行为差异、CUDA 版本兼容性问题反复出现。
- **显存管理靠手试**：Batch Size 要靠手动二分法试错，OOM 崩溃后丢失训练进度。
- **实验管理原始化**：超参靠手抄、命名靠脑记、对比靠截图，复现实验经常因为"忘了当时用的配置"而失败。
- **训练诊断黑箱化**：梯度消失/爆炸、过拟合、I/O 瓶颈等常见问题缺乏自动检测手段，多数人等到 loss 曲线异常才去翻 TensorBoard。
- **CV 专用工具缺失**：现有通用框架（MLflow、Weights & Biases）对 CV 场景的检测框可视化、混淆矩阵追踪、数据增强预览等缺乏原生支持。

### 1.2 目标

构建一个 **面向 CV 研究/学习场景的轻量级实验管理平台**，核心价值主张：

```
让研究者专注于模型和数据本身，而非工程琐事。
```

具体目标：

1. **零配置启动**：首次运行自动完成环境检测和加速选项推荐，新用户 5 分钟内跑通第一个实验。
2. **自动化工程决策**：批量大小、num_workers、精度格式等参数不再需要手动试错。
3. **可复现**：每个实验的代码、配置、环境、数据快照完整记录，一键复现。
4. **可诊断**：梯度健康、I/O 瓶颈、过拟合等常见问题自动检测并告警。
5. **可对比**：多实验指标、曲线、预测结果并排对比，辅助决策。

### 1.3 目标用户

| 用户画像 | 特点 | 核心需求 |
|----------|------|----------|
| CV 初学者 | 刚入门深度学习，用分类模型练手 | 自动配置推荐、错误提示友好、实验管理 |
| CV 研究者 | 发论文/做项目，需要大量消融实验 | 超参扫描、对比视图、可复现性 |
| CV 工程师 | 落地部署，关注模型效率 | 模型 Profiling、部署评估、数据集管理 |

---

## 2. 设计原则

### 2.1 非侵入 > 继承

平台不要求用户继承特定基类或改写模型接口。通过 **Hook 注入 + 外层包装** 实现接入，用户的 `nn.Module` 保持原生。

### 2.2 猜测并提示 > 静默假设

平台可以替用户做默认选择（自动探测 Batch Size、推荐 num_workers），但必须 **展示依据和预期收益**，让用户理解并随时覆盖。

### 2.3 容错 > 崩溃

训练过程中可预见的异常（OOM、梯度爆炸、磁盘满）应自动处理或给出明确恢复路径，而非直接崩溃丢失进度。

### 2.4 CV 优先

所有功能设计优先考虑 CV 场景的特殊需求：检测框/分割 Mask 的可视化、目标检测的 BBox 统计、小目标分析等。

### 2.5 渐进式复杂度

用户可以从最简单的单行 API 开始使用，随着需求增长逐步解锁高级功能（Sweep、贝叶斯优化、数据血缘追踪）。

---

## 3. 功能需求总览

```
┌────────────────────────────────────────────────────┐
│                    CVLab                           │
├────────────────────────────────────────────────────┤
│  训练前                    训练中        训练后    │
│  ┌──────────────────┐  ┌──────────┐  ┌───────────┐│
│  │ 环境检测          │  │ 梯度监控   │  │ 实验对比   ││
│  │ 加速选项面板      │  │ Loss诊断   │  │ 报告生成   ││
│  │ Batch Size 探测   │  │ 预测可视化 │  │ 模型Profile││
│  │ 数据I/O诊断      │  │ OOM恢复   │  │ 权重管理   ││
│  │ 数据增强预览     │  │ Checkpoint│  │ 数据集血缘 ││
│  │ Sweep 配置       │  │           │  │           ││
│  └──────────────────┘  └──────────┘  └───────────┘│
│                      │                            │
│              ┌───────┴───────┐                    │
│              │ 实验追踪中心   │                    │
│              │ (全过程记录)   │                    │
│              └───────────────┘                    │
└────────────────────────────────────────────────────┘
```

---

## 4. 模块一：系统环境检测与跨平台适配

### 4.1 启动时环境探针

**需求描述**：平台首次启动（或每次新建实验时）自动运行环境检测，输出环境档案。

**检测项清单**：

| 类别 | 检测项 | 用途 |
|------|--------|------|
| 系统 | OS 类型与版本 | 跨平台适配决策 |
| 系统 | Python 版本 | 依赖兼容性检查 |
| 系统 | PyTorch 版本 + CUDA 版本 | 特性可用性判断 |
| 系统 | 是否在 WSL2 环境 | GPU 访问虚拟化开销提示 |
| CPU | 型号、核心数、线程数 | num_workers 建议值 |
| CPU | 内存总量 / 可用量 | DataLoader 内存预算 |
| GPU | 型号、显存总量 | Batch Size 探测基础数据 |
| GPU | CUDA Compute Capability | 确定支持哪些加速特性 |
| GPU | Tensor Core 支持（Volta+） | FP16/INT8 加速可行性 |
| GPU | BF16 支持（Ampere+） | BF16 精度开关条件 |
| GPU | 驱动版本 / CUDA Runtime | 兼容性检查 |
| 兼容性 | PyTorch CUDA 编译版本 vs 系统 CUDA Runtime 版本 | 不匹配时告警，给出重装建议 |
| 多卡 | GPU 数量、NVLink/NVSwitch 拓扑（多卡互联方式） | 多卡单机场景的显存探测决策 |
| 存储 | 数据集目录磁盘类型（SSD/HDD） | I/O 瓶颈诊断 |
| 存储 | 可用磁盘空间 | 数据集、Checkpoint 空间预检 |

### 4.2 跨平台差异处理

| 检测场景 | 问题 | 平台行为 |
|----------|------|----------|
| Windows + `num_workers>0` | 缺少 `if __name__=='__main__'` 保护会报错 | 自动检测并生成带保护的代码模板 |
| Windows + `pin_memory` | 某些环境下会导致 hang | 提示风险，提供开关 |
| Windows vs Linux DataLoader | Windows 用 spawn，Linux 用 fork，性能差异显著 | 说明差异并给出平台特定建议值 |
| 文件路径分隔符 | 跨平台反斜杠/正斜杠不一致 | 统一用 `pathlib` 处理 |
| WSL2 + GPU | 通过虚拟化层访问 GPU，有约 10-15% 额外开销 | 检测到 WSL2 时提示性能损耗 |

### 4.3 训练加速选项面板

**需求描述**：检测完环境后展示交互式配置面板，用户按需勾选，平台生成对应代码注入训练流程。

**选项清单**：

| 加速技术 | 适用条件 | 预期收益 | 风险/备注 |
|----------|----------|----------|-----------|
| AMP FP16 | Tensor Core 支持 | 显存-40%，速度~2x | 小梯度可能下溢 |
| BF16 | Ampere+ | 显存-40%，速度~2x | 更稳定，但仅 Ampere+ |
| cuDNN Benchmark | 固定输入尺寸 | 自动选最优算法 | 首批次较慢 |
| torch.compile | PyTorch 2.0+ | 推理/训练加速 | 首次编译耗时 |
| Channels Last | CNN 模型 | 卷积层加速 | 部分操作不支持 |
| Gradient Checkpointing | 大模型/小显存 | 用计算换显存 | 约 20% 速度开销 |

**UI 展示规则**：
- 每个选项显示硬件兼容性状态（✅/⚠️/❌）
- 每项附带预期收益预估
- 不支持的功能灰色禁用，附原因说明
- DataLoader 参数自动推荐（num_workers=CPU 核心数，prefetch_factor=2）

---

## 5. 模块二：智能 Batch Size 管理

### 5.1 自动 Batch Size 探测

**需求描述**：训练开始前自动探测最大可用 Batch Size，避免手动试错。

**探测算法**（二分搜索 + 悲观数据注入 + optimizer.step + 安全余量）：

```
Step 0（前置）：使用与实验配置完全一致的精度格式
  - 用户开启 AMP FP16 → 探测时开启 torch.cuda.amp.autocast()
  - 用户开启 BF16 → 探测时使用 torch.bfloat16
  - 默认 FP32 → 探测时使用 FP32
  否则探测结果与实际训练不可比（FP32 与 FP16 显存差约 40%）

多卡单机模式（2-4 GPU）的额外处理：
  - 每卡独立计算显存，有效 Batch Size = 单卡 BS × GPU 数
  - 注意第 0 卡额外承担梯度同步开销，显存占用略高于其他卡——以第 0 卡为准
  - 学习率需线性缩放：lr' = lr × GPU 数（linear scaling rule）
  - 探测日志记录："检测到 2 张 GPU，有效 BS 翻倍，学习率自动 x2"

Step 1: 构造悲观数据
  - 分类：最大分辨率随机张量
  - 检测：每图 BBox=100（最耗显存，注意检测模型的匹配算法如匈牙利匹配可能导致二次级显存增长）
  - 分割：Mask=全 1（最稠密）

Step 2: 初始化优化器
  - 创建与用户配置一致的优化器（如 Adam，其动量状态占用额外显存）
  - 只靠 forward+backward 测不出峰值，optimizer.step() 才暴露完整显存占用

Step 3: 二分搜索
  low=1, high=根据显存总量估算的上限
  while low < high:
    mid = (low + high + 1) // 2
    执行前向+反向+优化器步进 N 次（默认 3）
    无 OOM → low = mid
    OOM    → high = mid - 1

Step 4: 安全余量
  推荐值 = low × 0.8（余量 20%，应对训练中显存波动）

Step 5: 清理
  显存缓存 + 删除临时优化器对象，避免干扰后续训练

Step 6: 记录
  探测过程的每个候选值 + 显存占用 + OOM 状态
```

**UI 展示**：

```
Batch Size 探测结果
─────────────────────────────────
模型：ResNet50   输入：3×224×224
显存总量：24 GB  可用：22.1 GB

  BS=32   显存占用：4.2 GB   ✅
  BS=64   显存占用：8.1 GB   ✅
  BS=128  显存占用：15.8 GB  ✅
  BS=192  显存占用：21.3 GB  ✅ ← 推荐（留 5% 余量）
  BS=256  显存占用：OOM       ❌

推荐 Batch Size：192
同时推荐开启 AMP 可进一步提升至约 256
─────────────────────────────────
```

### 5.2 梯度累积自动计算

**需求描述**：用户设定目标等效 Batch Size，平台自动计算梯度累积步数。

```
目标等效 Batch Size：256
实际可用 Batch Size：64
→ 自动设置 accumulation_steps = 4
```

### 5.3 OOM 崩溃感知与容错恢复

**需求描述**：训练中 OOM 不丢失进度，自动恢复。

> **设计决策**：原方案（进程内 try-catch OOM 后继续训练）在 DDP 下会因 AllReduce 通信失配导致死锁，不可行。改为子进程重启方案。

**约束说明**：重启训练子进程需要重新加载模型权重、初始化 CUDA context、扫描数据集目录建立索引。对数十万张图片的数据集，冷启动耗时可达数分钟。因此 OOM 恢复策略必须是 **有限次数 + 快速失败**。

**处理流程**：

```
1. 训练子进程崩溃，平台守护进程检测到退出码异常
2. 读取崩溃日志，解析 OOM 关键词
3. 如果是 OOM：
   a. 检查当前实验 OOM 历史计数器
   b. 首次 OOM：
      - batch_size 减小 20%
      - 从最近一次 Checkpoint 恢复
      - 启动新子进程重新训练
      - 记录："第 N 轮 OOM，BS X→Y，从 epoch M 恢复"
   c. 二次 OOM（递减尝试）：
      - batch_size 再减小 30%（累计 ~44%）
      - 记录："二次 OOM，进一步减小 BS"
   d. 三次 OOM：中断，打印内存泄漏诊断信息
      - 检查代码是否存在缓存了带计算图的 Tensor 而未 detach
      - 检查 DataLoader 是否泄露了内存
      - 提示用户开启 Gradient Checkpointing 或 AMP
4. 非 OOM 错误：停止，展示完整堆栈，不自动重试

DDP 场景：
  - 使用 torchrun 作为启动器，利用其原生容错重启
  - 监控所有 rank 的退出码，任意 rank 异常退出均视为失败
  - 不在 DDP 进程内做任何 try-catch 恢复尝试
```

> **关键假设**：Batch Size 探测阶段已通过悲观数据 + 20% 安全余量提供了保守推荐值。如果在此基础仍发生 OOM，大概率是代码存在内存泄漏（如缓存未 detach 的计算图 Tensor）而非单纯显存波动。递减尝试最多 2 次后应中断并指导用户排查。**重要**：探测阶段必须包含 optimizer.step()（详见 5.1 探测算法），否则真实峰值显存会被低估。

---

## 6. 模块三：非侵入式模型接入协议

### 6.1 设计哲学

```
❌ 不要求：继承 BaseClass、forward 返回特定格式 dict、改写模型代码
✅ 只做：在训练 loop 外层通过 Hook 注入接入
```

### 6.2 核心 API

```python
import cvlab

tracker = cvlab.Tracker(experiment_id="exp_042")

# 平台通过 Hook 注入，不需要动模型内部
tracker.watch(
    model,
    log_gradients=True,                # 用 register_full_backward_hook
    log_activations=True,              # 用 register_forward_hook
    watch_layers=["layer3", "layer4"], # 只监控指定层
    log_freq=50                        # 每 50 步记录一次
)

# 训练 loop 中只需要记录标量指标
for epoch in range(epochs):
    loss, acc = train_one_epoch(...)
    tracker.log({"train/loss": loss, "train/acc": acc}, step=epoch)
```

**非标量数据 Log API**：训练过程中的预测图片、混淆矩阵、检测框等 CV 专用数据通过以下扩展 API 记录：

```python
# 图片（分类预测样本、特征图、Grad-CAM）
tracker.log_image("pred_samples", image_grid, step=epoch, caption="Epoch 50 predictions")

# 混淆矩阵
tracker.log_confusion_matrix(
    y_true, y_pred,
    class_names=["cat", "dog", "bird"],
    step=epoch,
    normalize=True      # 按行归一化
)

# 检测框（叠加在原图上）
tracker.log_detection(
    "val_preds", image, boxes, scores, labels,
    class_names=["person", "car"],
    score_threshold=0.5,
    step=epoch
)

# 分割 Mask
tracker.log_segmentation(
    "val_seg", image, pred_mask, gt_mask,
    alpha=0.5,          # 叠加透明度
    step=epoch
)
```

**存储策略**：非标量数据不每 epoch 都保存，由 `log_freq` 统一控制。上述 API 仅在 `step % log_freq == 0` 时实际写入磁盘，其余调用被跳过。

### 6.3 Hook 分层设计

| Hook 类型 | 注册方式 | 用途 | 性能开销 |
|-----------|----------|------|----------|
| 前向 Hook | `register_forward_hook` | 特征图可视化、激活分布 | 中等：需保存特征图 |
| 后向 Hook | `register_full_backward_hook` | 梯度范数计算（梯度健康监控） | 低：只记录标量 |

**实现要点**：
- 通过 `watch_layers` 参数控制监控范围，避免全量注入造成显存暴涨
- 特征图数据仅在 `log_freq` 的倍数 step 保存到磁盘，其余 step 只记录梯度范数标量
- Hook 对象需在实验结束时显式 `remove()`，防止内存泄漏
- 用户传给 `tracker.log()` 的数据格式不限（标量 dict），平台只负责记录和展示

---

## 7. 模块四：实验追踪与版本控制

### 7.1 自动捕获的元信息

| 类别 | 自动捕获内容 |
|------|-------------|
| 身份 | 时间戳、唯一实验 ID |
| 代码 | Git commit hash（如存在）、训练脚本快照（内容哈希去重） |
| 配置 | 完整超参 JSON |
| 环境 | OS、GPU、PyTorch 版本、CUDA 版本 |
| 数据 | 数据集路径、样本总数、文件数 |
| 随机种子 | 实验种子号（如未指定则自动生成），四个随机源固定记录：`random`、`numpy`、`torch`、`cuda` |
| 命令 | 完整 CLI 命令（含参数） |

### 7.2 实验状态机

```
         ┌──→ 运行中 ──→ 完成
         │       │
创建 ────┤       ├──→ 失败（OOM / 梯度爆炸 / 用户中断 / 其他）
         │       │
         └──→ 排队中 ──→（等待 Sweep 调度）
         
完成/失败 → 已存档（手动标记，归档不常用实验）
```

**规则**：
- 失败的实验不删除，标记失败原因，可后续分析
- 用户中断视为一种明确的失败类型

### 7.3 实验标签与搜索

```
标签系统：
  #baseline  #lr-sweep  #augmentation-test  #overfitting  #待复现

搜索/过滤维度：
  - 标签
  - 超参范围：lr > 0.001 AND batch_size = 64
  - 指标范围：val_acc > 0.85
  - 时间范围
  - 状态（只看失败的）
  - 自由文本搜索
```

### 7.4 随机种子管理

**需求描述**：实验可复现性的基础设施，平台自动管理所有随机源。

```python
# 实验开始前平台自动调用（用户无感知）
def seed_everything(seed: int):
    random.seed(seed)
    np.random.seed(seed)
    torch.manual_seed(seed)
    torch.cuda.manual_seed_all(seed)
```

**与加速选项的冲突提示**：`cuDNN Benchmark` 和 `cuDNN Deterministic` 在复现性上有冲突——加速面板（4.3）中需要提示用户：

> cuDNN Benchmark = True 时，每次运行因算法选择不同而无法严格复现。
> 如需完全复现，请关闭 Benchmark 并开启 `torch.backends.cudnn.deterministic = True`（速度损失约 10%）。

### 7.5 一键复现

```bash
# 复现实验 exp_042
# 环境：Ubuntu 22.04 / RTX 3090 / PyTorch 2.1
python train.py \
  --lr 0.001 \
  --batch_size 128 \
  --epochs 100 \
  --model resnet50 \
  --seed 42
# 代码版本：git checkout a3f2c1d
```

**复现前检查**：
- 当前环境与原始环境差异提示
- 数据集变化检测（利用数据集血缘模块）
- 依赖版本兼容性检查

---

## 8. 模块五：训练过程诊断（CV 专项）

### 8.1 梯度健康监控

**需求描述**：实时监控各层梯度范数，自动检测梯度消失/爆炸。

> **性能约束**：`register_full_backward_hook` 在每层上同步计算 L2 范数会强制打断 CUDA kernels 的异步执行流水线，导致非预期的性能损失。因此梯度监控必须采用 **采样模式**，而非每步全量计算：
> - **采样频率**：默认每 50 step 采集一次（`log_freq=50`），可配置
> - **监控范围**：仅监控关键层（`watch_layers` 参数指定，如第一个卷积层 + 最后一个分类层），而非全模型
> - **粒度控制**：采样 step 计算完整 L2 范数，非采样 step 仅捕获聚合统计信息（通过更轻量的方式）

```
每 50 step 记录各层梯度 L2 范数（采样模式）：

Layer              grad_norm   状态
───────────────────────────────────
layer4.conv.weight  0.023       ✅ 正常
layer3.conv.weight  0.018       ✅ 正常
layer2.conv.weight  0.0003      ⚠️ 偏低
layer1.conv.weight  0.00001     ❌ 梯度消失
```

**告警规则**：

| 条件 | 判定 | 告警级别 |
|------|------|---------|
| grad_norm < 1e-5 | 梯度消失 | 警告 |
| grad_norm > 10 | 梯度爆炸 | 警告 |
| Loss 连续 N epoch 不降 | 训练停滞 | 警告 |
| Loss 单步跳升 > 阈值 | Loss Spike | 信息（标记该 step） |

### 8.2 Loss 异常检测

```
Loss 曲线分析（自动运行）：

✅ 训练 loss 正常下降
⚠️ 第 23 epoch 检测到 loss spike（3.2 → 8.7）
   → 可能原因：学习率过大 / 异常样本 / 梯度爆炸
   → 建议：检查 lr scheduler、开启梯度裁剪

❌ 验证 loss 从第 15 epoch 开始持续上升
   → 判断：过拟合
   → 建议：增大正则化、增强数据增强、减小模型容量
```

### 8.3 预测可视化（训练时间轴）

**需求描述**：每 N 个 epoch 对固定验证样本做预测并保存，形成训练过程时间轴。

**分类任务**：
```
Epoch 10：[图片] → cat (0.72) | 真实：cat ✅
Epoch 30：[图片] → cat (0.95) | 真实：cat ✅
Epoch 50：[图片] → dog (0.61) | 真实：cat ❌
```

**检测任务**：
- 保存预测框随训练的变化，可视化框的收敛过程
- 自动检测框漂移现象

### 8.4 混淆矩阵动态追踪

- 每个 checkpoint 保存混淆矩阵
- 可回放：哪些类别最先学会，哪些一直搞混
- 展示类别间的混淆模式变化

### 8.5 学习率与 Loss 联动分析

- Loss 曲线上叠加显示 lr 变化
- 自动标注 lr 下降时刻与 loss 响应的关系
- 在平台期提示建议（降 lr / early stopping）

---

## 9. 模块六：超参管理与调优

### 9.1 配置方式（三选一）

| 方式 | 适用场景 | 示例 |
|------|----------|------|
| Python DSL | 简单实验 | `exp.config(lr=0.001, batch_size=64)` |
| YAML 文件 | 复杂实验 | `config.yaml` 完整配置 |
| UI 填写 | 快速实验 | Web 表单直接填写 |

### 9.2 超参扫描（Sweep）

**搜索策略**：

| 策略 | 适用场景 | 说明 |
|------|----------|------|
| Grid Search | 小搜索空间 | 穷举所有组合 |
| Random Search | 中等/大空间 | 比 Grid 更高效（经验法则），默认策略 |

> **注**：贝叶斯优化暂不支持。单卡场景下的 trial 数通常不足以有效拟合高斯过程代理模型，实现复杂但收益有限，计划后续版本引入。

**Sweep 配置示例**：

```yaml
# sweep_config.yaml
strategy: random

params:
  lr:
    distribution: log_uniform
    min: 0.0001
    max: 0.1
  batch_size:
    values: [32, 64, 128]  # 自动过滤超显存值（详见下方过滤规则）
  optimizer:
    values: [Adam, SGD]

target_metric: val_acc
max_trials: 50
max_concurrent: 2  # 根据显存自动限制
early_stop:
  patience: 10      # 连续 10 次无提升则停止整个 Sweep
  min_trials: 20    # 最少跑 20 次
```

**Batch Size 过滤规则**：
- 过滤前先运行一次探测（如果尚未探测）
- 过滤掉超过推荐上限的值，保留其余值继续扫描
- 如果所有候选值均超显存：中断 Sweep 并提示用户
- 过滤结果展示给用户确认后再开始扫描

### 9.3 超参重要性分析

**需求描述**：完成 Sweep 后自动分析各超参对目标指标的影响程度。

```
超参重要性分析（基于 16 个实验）
────────────────────────────────
学习率        ████████████  82%  影响最大
优化器        ████          28%
Batch Size    ██            14%  几乎无影响

结论：lr 是最关键的超参，建议下一轮专注精细搜索。
```

**实现方式**：基于随机森林的特征重要性或 fANOVA 分析。

---

## 10. 模块七：数据集管理

### 10.1 支持格式

| 任务类型 | 导入格式 |
|----------|----------|
| 分类 | ImageFolder 目录结构 / CSV 标签文件 |
| 检测 | COCO JSON / Pascal VOC XML / YOLO TXT |
| 分割 | COCO JSON / 自定义 Mask 目录 |

**统一处理**：所有格式统一转换为平台内部格式，训练时按需导出。

### 10.2 数据集分析报告

**通用分析**：
- 类别分布图（饼图 + 柱状图），标注不平衡比例
- 样本总数、train/val/test 分布
- 重复图片检测（基于感知哈希）
- 近似重复检测（相似度 > 95%）

**检测任务专项**：
- BBox 尺寸分布（小目标比例分析）
- 每张图平均目标数
- 宽高比分布（影响 anchor 设计）
- 标注质量检测（空标注、异常尺寸）

**分割任务专项**：
- 各类别像素占比
- Mask 与图片对齐验证

### 10.3 数据增强可视化预览

```
┌────────────────────────────────────────────┐
│  原始图片          增强后（每次刷新随机）   │
│  [图片]            [图片] [图片] [图片]     │
│                                            │
│  RandomHorizontalFlip  p=[ 0.5 ]          │
│  ColorJitter brightness=[ 0.2 ] sat=[ 0.1]│
│  RandomCrop size=[ 224 ]                   │
│  RandomRotation degrees=[ 15 ]            │
│                                            │
│  [应用到实验] [查看更多样本]               │
└────────────────────────────────────────────┘
```

---

## 11. 模块八：Checkpoint 管理

### 11.1 保存策略

```
保存策略（可多选）：
  ✓ 保存最优（按 val_acc / val_loss / mAP 等指标）
  ✓ 保存最后一个 epoch
  ○ 每 N 个 epoch 保存一次
  ○ 保存前 K 个最优
  ✓ 自动清理旧 checkpoint（保留最近 N 个）
```

### 11.2 训练中断处理

**需求描述**：训练在 epoch 中间被中断（如掉电、进程被杀）时，明确处理策略。

```
默认策略（epoch 粒度）：
  - 每 epoch 结束时保存一次 checkpoint
  - 中断后从上一个完整 epoch 的 checkpoint 恢复
  - 当前 epoch 内已训练的 step 全部丢失
  - 适用于大多数场景，I/O 开销低

可选策略（step 粒度，进阶）：
  - 每 N 个 step 保存一次 step-level checkpoint
  - 中断后从最近的 step checkpoint 恢复，丢失 < N step
  - 代价：磁盘 I/O 和显存带宽占用增加
  - 建议：仅在训练成本极高（单 epoch > 24h）时使用

推荐方案：
  - 默认 epoch 粒度
  - 如果用户认为风险不可接受，可切换到 step 粒度
  - 平台在创建实验时提示权衡
```

### 11.3 EMA 权重处理

**需求描述**：检测用户代码中是否存在 EMA（指数移动平均）对象，如果存在则单独管理 EMA 权重。

```
EMA 处理规则：
  - 自动检测用户训练代码中是否包含 EMA 对象（通过检查 model 是否有 ema 属性或用户显式注册）
  - 如果存在 EMA：
    ○ 每个 checkpoint 同时保存原始权重和 EMA 权重
    ○ 命名区分：best.pt（原始）/ best_ema.pt（EMA）
    ○ 评估和 Profiling 默认使用 EMA 权重（如果存在）
  - 用户可通过 API 显式注册 EMA 对象：
    tracker.register_ema(ema_model)
```

### 11.4 Checkpoint 对比

同一实验的不同 epoch checkpoint 在 UI 上直接对比指标：

```
Epoch 30 (val_acc: 85.2%)  vs  Epoch 50 (val_acc: 88.7%)

类别 "cat"：  82% → 91%   ↑
类别 "dog"：  79% → 83%   ↑
类别 "bird"： 91% → 88%   ↓ ← 值得关注
```

---

## 12. 模块九：实验对比与报告

### 12.1 多实验并排对比

```
                    exp_01    exp_02    exp_03 ★ Best
─────────────────────────────────────────────────────
学习率             0.001     0.01      0.001
优化器             Adam      Adam      SGD        ← 差异高亮
Batch Size         64        64        128
数据增强           weak      weak      strong     ← 差异高亮
─────────────────────────────────────────────────────
最优 val_acc       87.3%     85.1%     89.2% ★
最低 val_loss      0.31      0.38      0.27  ★
训练时长           2h10m     2h08m     2h45m
─────────────────────────────────────────────────────
```

**交互规则**：
- 相同超参值自动折叠隐藏
- 差异列高亮标记
- 最优值自动标注 ★

### 12.2 曲线叠加对比

- 多实验 loss/acc 曲线叠加在同一图上
- 支持滑动平均平滑
- 支持按 best epoch 对齐
- 点击曲线上任意一点 → 显示该点完整超参

### 12.3 一键报告生成

**输出格式**：HTML（自包含，可分享）

**报告包含**：
```
实验总结报告
├── 实验目标（用户填写）
├── 环境信息（自动）
├── 实验对比表（自动）
├── 最优实验 loss/acc 曲线（自动）
├── 混淆矩阵（自动）
├── 代表性样本预测（自动）
├── 关键发现（自动生成文字总结，当前：规则模板生成；后续可扩展为 LLM 驱动）
│   "exp_03 取得最优（val_acc=89.2%），相比 baseline 提升 1.9%，
│    主要差异在于使用了 SGD 优化器和更强的数据增强。"
└── 超参重要性分析（自动，Sweep 后）
```

---

## 13. 模块十：数据 I/O 瓶颈诊断

### 13.1 训练 Pipeline 健康检查

**需求描述**：训练开始时自动检测是否存在 I/O 瓶颈（GPU 等数据）。

```
训练 Pipeline 健康检查（前 100 个 step）

时间分解：
  数据加载（CPU）   ████████████████ 68%  ← 瓶颈！
  GPU 前向计算      ███ 18%
  GPU 反向传播      ██ 10%
  其他               ▌ 4%

GPU 利用率：█████░░░░░ 45%  ← 过低，CPU 限速

建议（按优先级）：
① num_workers 2→8（CPU 有 16 核）
② 数据集在 HDD 上，强烈建议迁移到 SSD
③ 图片 > 10 万张，考虑 LMDB 格式（读取速度 3-5x）
④ 开启 pin_memory=True
```

### 13.2 存储类型自动检测

```python
# Linux：读取 /sys/block/<dev>/queue/rotational（0=SSD, 1=HDD）
# Windows：通过 WMI 查询 MediaType
```

**输出**：
```
数据集路径：/data/imagenet
磁盘类型：HDD（机械硬盘）⚠️
随机读取速度：~120 MB/s（估算）
建议：CV 训练中大量小图片随机读取对 HDD 压力极大，强烈建议使用 SSD
```

---

## 14. 模块十一：模型 Profiling

### 14.1 需求描述

实验完成后，通过独立 CLI 命令生成模型物理性能报告，评估部署可行性。

> **关键约束**：训练刚结束时 GPU 处于热状态（Thermal Throttling），此时直接在训练进程中测得的 FPS/延迟数据不可信。Profiling 必须在独立进程中运行，确保 GPU 温度回落后、显存完全清空。

```bash
cvlab profile --checkpoint best.pt --model resnet50 --input 3x224x224
```

### 14.2 Model Card 内容

```
模型物理性能报告（基于 best checkpoint）
────────────────────────────────────────────
模型：ResNet50   输入：3×224×224

参数量
  总参数：25.6M
  可训练：25.6M（全量微调）
  冻结：0

计算量
  FLOPs：4.1G（每次前向）
  MACs：2.05G

推理性能（torch.no_grad + cudnn.benchmark）
  Batch=1   延迟：4.2ms    FPS：238
  Batch=32  延迟：18.6ms   FPS：1720 ← 最优吞吐
  Batch=64  延迟：35.1ms   FPS：1822

显存占用（推理）
  Batch=1：1.1 GB
  Batch=32：2.8 GB

适用场景评估
  实时推理（>30 FPS）： ✅
  边缘设备（<100M）：   ✅
  手机端（<10M）：      ❌
```

---

## 15. 模块十二：预训练权重管理

### 15.1 权重加载诊断（P1）

```
加载预训练权重时，自动检测并报告：

✅ backbone.layer1.weight   形状匹配，成功加载
✅ backbone.layer2.weight   形状匹配，成功加载
⚠️ classifier.weight       形状不匹配（1000→10），已随机初始化
❌ neck.fpn_conv.weight     Key 不存在于预训练权重中

建议：
- strict=False 忽略不匹配 Key
- 分类头不匹配通常因数据集迁移，正常
- Neck 层缺失请确认权重版本是否匹配
```

### 15.2 本地权重缓存（P2）

- 管理 `~/.cvlab/weights/` 目录
- 首次下载后缓存，避免多实验重复下载
- 记录来源 URL、文件哈希、下载时间
- CLI：`cvlab weights list` / `cvlab weights download resnet50`

---

## 16. 模块十三：数据集版本与血缘管理

### 16.1 需求背景

数据修改后，历史实验无法严格复现——这是影响实验可复现性的一个常见但常被忽略的问题。

### 16.2 数据集快照

```
每个实验记录的数据集信息：

  数据集路径：/data/my_dataset
  样本总数：12,483
  记录时间：2025-05-18 14:32
  
  标注文件 SHA256（精确检测用）：a3f2c1...
  根目录统计（快速检测用）：总大小=12.4GB, 总文件数=12,483
```

### 16.3 复现时一致性检查

```
⚠️ 数据集已发生变化
历史实验（exp_042）的数据集与当前不一致
变化：新增 1,247 个文件，修改 83 个文件

继续复现，结果可能与原实验不同。
[继续（仅供参考）]  [取消]
```

### 16.4 变更检测策略

> **关键约束**：CV 数据集常有数万至数百万级文件。在 NTFS 文件系统上逐文件执行 stat() 是 $O(N)$ 操作，每次训练启动前全量扫描会导致数分钟卡顿，不可接受。

**策略**（分两级）：

| 级别 | 检测方式 | 成本 | 精度 |
|------|----------|------|------|
| 快速检测（默认） | 数据集根目录统计：总大小 + 文件总数 + 修改时间 | $O(1)$，毫秒级 | 粗粒度：无法检测文件替换 |
| 精确检测（可选） | 仅对标注文件（COCO JSON / VOC XML / YOLO TXT）做 SHA256 | $O(M)$，M≈1-10，快速 | 细粒度：标注变更即可检测 |

**匹配规则**：
- 快速检测不一致 → 提示"数据集可能已变化"，建议执行精确检测
- 精确检测不一致 → 阻止复现，要求用户确认
- 快速检测一致 → 认为数据集未变（可接受的低精度权衡）

**路径变更处理**：数据集从 HDD 迁移到 SSD（或目录改名）后路径变化但内容不变，血缘系统需要正确处理。

```
路径变更但内容未变的场景：
  1. 检测到路径不匹配，先对新路径执行精确检测（标注文件哈希）
  2. 如果哈希一致 → 自动更新实验记录中的数据集路径，不报警
  3. 如果哈希不一致 → 报警，阻止复现

一键复现时的路径覆盖：
  - 允许用户手动覆盖数据集路径
  - 与原路径不同时提示："请确认新路径下是否为同一数据集"
  - 覆盖后执行精确检测确认
```

---

## 17. 非功能性需求

### 17.1 性能

| 指标 | 目标值 |
|------|--------|
| 环境检测耗时 | < 5 秒 |
| Batch Size 探测耗时 | < 30 秒 |
| 实验查询（1000 条） | < 200ms |
| 曲线渲染（10 条叠加） | < 500ms |
| Hook 注入对训练速度影响 | 仅梯度监控（采样模式，50 step/次）：< 3%；梯度+特征图（采样模式）：< 8%；全量每步（不建议）：> 20% |
| 报告生成 | < 10 秒 |

### 17.2 存储

| 数据 | 估算 |
|------|------|
| 单实验元数据 | ~50KB |
| 单实验指标序列（100 epoch，每 epoch 10 个标量） | ~20KB |
| 单次特征图保存 | ~10MB（按需） |
| 单实验总存储（不含 Checkpoint） | ~100MB |
| 实验数据库（1000 实验） | ~100MB |

### 17.3 兼容性

| 平台 | 支持状态 |
|------|----------|
| Windows 10/11 | P0 |
| Ubuntu 20.04+ | P0 |
| macOS | P1（仅 CPU 模式） |
| WSL2 | P0（GPU 访问提示） |
| PyTorch 2.0+ | P0 |
| PyTorch 1.12-1.13 | P1（部分功能受限） |

### 17.3a 无 GPU 时的降级行为

环境探针检测到无 GPU 时，平台自动执行以下降级策略：

```
检测到无 GPU → 自动降级模式：

环境检测：
  - 跳过 GPU 相关检测项（显存容量、CUDA 版本、Compute Capability）
  - 加速选项面板禁用所有 GPU 加速项：
    ❌ AMP FP16 / BF16（无不支持）
    ❌ torch.compile（CPU 收益有限，跳过）
    仅保留 DataLoader 优化项（num_workers、prefetch_factor）

Batch Size 探测：
  - 不执行显存基线的前向+反向探测
  - 改为基于系统 RAM 的简化估算（CPU 训练显存不限，但 BS 过大影响收敛）
  - 推荐值 = min(根据 RAM 估算, 256)，以收敛安全性为先

其他功能：
  - 梯度监控、Loss 诊断、实验追踪、数据 I/O 诊断等均正常可用
  - 模型 Profiling 不测 GPU 延迟，只报告 FLOPs/MACs/参数量
  - Checkpoint 管理不受影响
```

### 17.4 可扩展性

- 实验存储后端可插拔（默认 SQLite → 可选 PostgreSQL）
- 支持自定义 Hook 扩展（用户可注册自定义回调）
- 支持分布式训练场景（DDP/torchrun）

### 17.5 安全性

- 本地运行为主，不上传用户数据到外部服务
- 实验数据存储在用户指定目录，不强制写入系统目录
- 如有 Web UI，默认绑定 localhost 而非 0.0.0.0

---

## 18. 功能优先级矩阵

```
P0（MVP 核心 —— 必须有）
┌────────────────────────────────────────────────────┐
│  模块                          交付标准             │
├────────────────────────────────────────────────────┤
│  环境检测与加速选项面板          完整检测 + UI      │
│  Batch Size 探测（含悲观数据）   二分搜索 + 推荐    │
│  非侵入式 Hook 接入（watch API） 前向/后向 Hook    │
│  实验追踪（超参+指标+自动元信息） 记录与持久化      │
│  OOM 崩溃感知+子进程重启         守护进程模式       │
│  Checkpoint 管理（保存+恢复）    策略配置+恢复      │
│  实验列表与基础对比              并排对比表         │
└────────────────────────────────────────────────────┘

P1（第二阶段 —— 体验提升）
┌────────────────────────────────────────────────────┐
│  模块                          交付标准             │
├────────────────────────────────────────────────────┤
│  数据 I/O 瓶颈诊断               Pipeline 分析      │
│  梯度健康监控与异常告警          实时监控+告警      │
│  Loss 异常自动诊断               Loss 曲线分析      │
│  预测可视化（训练时间轴）        固定样本跟踪       │
│  预训练权重加载诊断              Key 匹配检测       │
│  模型 Profiling（FLOPs/FPS）    Model Card          │
│  超参扫描（Grid/Random）         Sweep 调度         │
│  混淆矩阵动态追踪                Checkpoint 级保存  │
│  曲线叠加对比                    多实验叠加         │
└────────────────────────────────────────────────────┘

P2（第三阶段 —— 差异化亮点）
┌────────────────────────────────────────────────────┐
│  模块                          交付标准             │
├────────────────────────────────────────────────────┤
│  数据集版本血缘追踪              标注文件哈希+统计   │
│  超参重要性分析                  sklearn 随机森林   │
│  超参扫描 Grid 扩展              Random Search      │
│  数据增强可视化预览              交互式预览         │
│  数据集分析报告                  类别分布+质量检测  │
│  预训练权重缓存管理              本地缓存池         │
│  学习率与 Loss 联动分析          叠加图表           │
│  一键报告生成                    HTML 报告          │
│  标签与高级搜索                  多维过滤           │
└────────────────────────────────────────────────────┘

> **后续版本（已移除）**：贝叶斯超参搜索（单卡场景 trial 数不足以拟合高斯过程，实现复杂收益低）
```

---

## 19. 技术风险与关键决策

### 19.1 已知风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Hook 注入增加显存占用 | 影响 Batch Size 探测准确性 | watch_layers 限制监控范围，梯度监控采用采样模式而非每步全量 |
| 梯度 Hook 打断 CUDA 异步流水线 | 性能损失 > 文档预估的 5% | 梯度监控转为采样模式（默认每 50 step），仅在关键层挂载 Hook |
| 子进程重启冷启动时间长 | 数十万张图片的数据集重载模型+重建索引耗时数分钟 | 递减尝试策略：最多重试 2 次，第三次直接中断并输出内存泄漏诊断 |
| Profiling 热失真 | GPU 降频导致 FPS 数据不可信 | Profiling 改为独立 CLI 命令，在独立进程中运行 |
| 数据血缘哈希在 NTFS 下 I/O 过重 | 全量文件 stat() 耗时数分钟 | 默认使用 O(1) 根目录统计，可选精确模式仅哈希标注文件 |
| Windows 上多进程 DataLoader 兼容 | spawn 启动慢，debug 困难 | 自动检测并提示，模板代码中预置 `if __name__` |
| 不同 GPU 型号探测结果迁移性 | 同一配置在 3090 和 A100 表现不同 | 环境检测信息随实验保存，复现时对比提示 |

### 19.2 实现方式决策

| 决策点 | 方案 | 理由 |
|--------|------|------|
| 实验存储 | SQLite（默认）+ PostgreSQL（可选） | 个人用户无需部署 DB，团队场景可扩展 |
| 前端 | Streamlit / Gradio（强制锚定） | 纯 Python 直接绑定 SQLite + 生成交互式图表，避免传统前后端分离（Vue+FastAPI）消耗 70% 精力在接口联调上。CLI 模式同期保持，所有功能可脱离 UI 使用 |
| 实时监控 | Streamlit 轮询模式（~10 秒刷新） | Streamlit 非 WebSocket 推送，实时曲线采用 auto-refresh 每 10 秒轮询；高实时性告警（梯度爆炸等）走命令行输出而非 UI 推送；接受"准实时"而非严格实时 |
| 进程管理 | `subprocess` + `torchrun` | 原生支持 DDP 容错 |
| 配置文件 | YAML | 可读性好，支持复杂嵌套 |
| 数据血缘变更检测 | 根目录 O(1) 统计（默认）+ 标注文件 SHA256（精确） | CV 数据集文件量太大，全量内容哈希不可行；NTFS 下全量 stat() 不可接受 |

---

## 附录 A：与竞品的差异化定位

| 对比维度 | CVLab | MLflow | W&B | TensorBoard |
|----------|-------|--------|-----|-------------|
| CV 专项支持 | ✅ 检测框/分割可视化、混淆矩阵 | ❌ | ❌ | ❌ |
| 自动环境检测 | ✅ 完整探针 | ❌ | ❌ | ❌ |
| Batch Size 探测 | ✅ 悲观+二分搜索+安全余量 | ❌ | ❌ | ❌ |
| 训练诊断 | ✅ 梯度/Loss/OOM 自动告警 | ❌ | ❌ | ✅ 基础曲线 |
| I/O 瓶颈检测 | ✅ Pipeline 分析 | ❌ | ❌ | ❌ |
| 离线可用 | ✅ 全本地 | ✅ | ❌ 需云端 | ✅ |
| 模型 Profile | ✅ FLOPs/FPS/参数量 | ❌ | ❌ | ❌ |
| 超参重要性 | ✅ 自动分析 | ❌ | ✅ | ❌ |
| 学习成本 | 低（单行 API） | 中 | 中 | 低 |

---

## 附录 B：术语表

| 术语 | 说明 |
|------|------|
| AMP | Automatic Mixed Precision，自动混合精度训练 |
| OOM | Out of Memory，显存耗尽 |
| DDP | Distributed Data Parallel，分布式数据并行 |
| Sweep | 超参扫描，自动调度多组实验搜索最优超参 |
| Hook | PyTorch 的模块钩子机制，可在不修改代码的情况下插入监控 |
| Model Card | 模型物理性能卡片，包含参数量、FLOPs、推理延迟等 |
| Data Provenance | 数据血缘，追踪数据集的版本和变更历史 |
| Gradient Checkpointing | 梯度检查点，用额外计算换取更低显存占用 |
| Channels Last | 内存格式优化，将 NCHW 转换为 NHWC |
